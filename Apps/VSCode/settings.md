# Hotkeys for General Settings

### Open keyboard shortcuts GUI
`(⌘)cmd k + (⌘)cmd s`

### Open keyboard shortcuts JSON
`(⌘)cmd k + (⌘)cmd k`

### Open Settings GUI
`(⌘)cmd + .`

### Open Settings JSON
`(⌘)cmd k + (⌘)cmd .`
